---
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import DetailHero from '../components/DetailHero.astro';
import '../styles/global.css';

interface Props {
  title: string;
  subtitle?: string;
  heroImage: string;
  actLabel?: string;
}

const { 
  title,
  subtitle,
  heroImage,
  actLabel,
} = Astro.props;

const pageTitle = `${title} — Time Traveler's Itinerary`;
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={`${title} - Japan 2026 trip details`} />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <title>{pageTitle}</title>
  </head>
  <body class="min-h-screen">
    <Header />
    
    <DetailHero 
      title={title}
      subtitle={subtitle}
      heroImage={heroImage}
      actLabel={actLabel}
    />

    <main class="max-w-4xl mx-auto px-6 py-12">
      <slot />
    </main>

    <Footer />
    
    <script>
      // Section reveal animation using Intersection Observer
      document.addEventListener('DOMContentLoaded', () => {
        const revealElements = document.querySelectorAll('.section-reveal');
        
        const observer = new IntersectionObserver((entries) => {
          entries.forEach(entry => {
            if (entry.isIntersecting) {
              entry.target.classList.add('visible');
              observer.unobserve(entry.target);
            }
          });
        }, {
          threshold: 0.1,
          rootMargin: '0px 0px -50px 0px'
        });

        revealElements.forEach(el => observer.observe(el));
      });
    </script>
  </body>
</html>
